<!DOCTYPE html>
<html>
	<head>
		<title>RIFTAuctionManagement</title>
		<%= stylesheet_link_tag :all %>
		<%= javascript_include_tag :defaults %>
		<%= csrf_meta_tag %>
	</head>
	<body>
		<%= render 'shared/header'%>
	
		<%= yield %>
		<%= debug(params) if Rails.env.development? %>
	</body>
</html>
